#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Start the local Home Assistant dev instance (official container image, see
# compose.yaml) with logs attached. The integration source is bind-mounted;
# run scripts/restart after Python code changes.
docker compose up
